<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>מולטיבראון - לוקיישנים שווים במינימום מאמץ</title>
    <meta name="description" content="משנים את עולם החיפוש: יותר אישי, יותר ממוקד, פחות כאבי ראש.">
    <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@300;400;500;600;700;800&family=Playfair+Display:wght@700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        :root {
            --primary-bg: linear-gradient(135deg, #00E0FF, #2D7FFF, #A06BFF);
            --secondary-bg-glass: rgba(255, 255, 255, 0.15);
            --accent-primary: #FF5EA1; /* ורוד-קוראל */
            --accent-secondary: #00E0FF; /* טורקיז */
            --white: #FAFAFA;
            --text-dark: #1a0b2e; /* צבע טקסט כהה לרקע הבהיר */
            --text-light-on-dark: #FAFAFA;
            --light-grey: #444; /* אפור לטקסט משני */
            --font-display: 'Playfair Display', serif;
            --font-body: 'Heebo', sans-serif;
        }
        body { 
            font-family: var(--font-body); 
            background: var(--primary-bg); 
            color: var(--text-dark); /* Changed to dark text */
            line-height: 1.6; 
            overflow-x: hidden; 
        }
        h1, h2, h3, h4, h5, h6 { font-family: var(--font-display); font-weight: 700; }
        
        header { 
            position: fixed; top: 0; left: 0; right: 0; z-index: 1000; 
            background: rgba(255, 255, 255, 0.1); 
            backdrop-filter: blur(20px); 
            border-bottom: 1px solid rgba(255, 255, 255, 0.2); 
            transition: all 0.3s ease; 
        }
        header.scrolled { 
            background: rgba(255, 255, 255, 0.2); 
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1); 
        }
        nav { max-width: 1400px; margin: 0 auto; padding: 1.5rem 3rem; display: flex; justify-content: space-between; align-items: center; }
        .logo { display: flex; align-items: center; gap: 1rem; text-decoration: none; }
        .logo img { width: 45px; height: 45px; }
        .logo span { font-size: 1.5rem; font-weight: 700; color: var(--text-dark); font-family: var(--font-display); }
        .nav-links { display: flex; gap: 2.5rem; align-items: center; }
        .nav-links a { color: var(--text-dark); text-decoration: none; font-size: 0.95rem; transition: opacity 0.3s; font-weight: 500;}
        .nav-links a:hover { opacity: 0.6; }
        .nav-cta { 
            background: var(--accent-primary); 
            color: var(--white) !important; 
            padding: 0.7rem 1.8rem; 
            border-radius: 6px; 
            font-weight: 600; 
            box-shadow: 0 4px 15px rgba(255, 94, 161, 0.4);
            transition: all 0.3s ease;
            text-decoration: none;
        }
        .nav-cta:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(255, 94, 161, 0.6);
        }
        
        .hero { height: 100vh; position: relative; display: flex; align-items: center; justify-content: center; text-align: center; overflow: hidden; }
        .hero-bg { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
        .hero-bg::before { content: ''; position: absolute; top: 0; left: 0; right: 0; bottom: 0; background: linear-gradient(135deg, rgba(0, 224, 255, 0.1), rgba(45, 127, 255, 0.1), rgba(160, 107, 255, 0.2)); z-index: 1; }
        .hero-slide { position: absolute; width: 100%; height: 100%; opacity: 0; transition: opacity 1.5s; }
        .hero-slide.active { opacity: 1; }
        .hero-slide img { width: 100%; height: 100%; object-fit: cover; filter: brightness(0.6); }
        .hero-content { 
            position: relative; z-index: 2; max-width: 1000px; padding: 0 2rem; 
            animation: fadeInUp 1s; 
        }
        @keyframes fadeInUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
        .hero h1 { font-size: clamp(3rem, 7vw, 6rem); font-weight: 800; line-height: 1.1; margin-bottom: 1.5rem; color: var(--white); text-shadow: 0 2px 20px rgba(0,0,0,0.3);}
        .hero h2 { font-size: clamp(1.1rem, 2vw, 1.4rem); max-width: 700px; margin: 0 auto 3rem; font-weight: 400; color: var(--white); }
        .hero-cta { display: inline-block; background: var(--accent-primary); color: var(--white); padding: 1rem 2.5rem; border-radius: 6px; text-decoration: none; font-weight: 500; transition: all 0.3s; box-shadow: 0 4px 20px rgba(255, 94, 161, 0.4); }
        .hero-cta:hover { transform: translateY(-2px); box-shadow: 0 10px 30px rgba(255, 94, 161, 0.6); }
        
        section { padding: 6rem 3rem; }
        .section-header { max-width: 800px; margin: 0 auto 4rem; text-align: center; }
        .section-header h2 { 
            font-size: clamp(2rem, 4vw, 3rem); font-weight: 700; margin-bottom: 1rem; 
            color: var(--text-dark);
        }
        .section-header p { font-size: 1.15rem; color: var(--light-grey); }
        
        .locations { background: rgba(255, 255, 255, 0.1); }
        .locations-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 2rem; max-width: 1400px; margin: 0 auto; }
        .location-card-link {
            text-decoration: none;
            color: var(--text-dark);
            display: block;
            border-radius: 8px;
            overflow: hidden;
            transition: transform 0.4s, box-shadow 0.4s;
            background: var(--secondary-bg-glass);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        .location-card-link:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0,0,0,0.2);
        }
        .location-card { 
            position: relative; 
            overflow: hidden; 
            aspect-ratio: 4/5; 
            cursor: pointer; 
        }
        .location-card img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.6s; filter: brightness(0.9); }
        .location-card-link:hover .location-card img { transform: scale(1.05); }
        .location-overlay { position: absolute; bottom: 0; left: 0; right: 0; padding: 2rem; background: linear-gradient(0deg, rgba(0,0,0,0.7), transparent); }
        .location-card h3 { font-size: 1.5rem; font-weight: 600; margin-bottom: 0.5rem; color: var(--white); }
        .location-card p { font-size: 0.95rem; opacity: 0.9; color: var(--white); }
        
        .features { background: transparent; }
        .features-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 3rem; max-width: 1200px; margin: 0 auto; }
        .feature { text-align: center; }
        .feature-icon { 
            font-size: 2.5rem; margin-bottom: 1.5rem; 
            color: var(--accent-primary);
        }
        .feature h3 { font-size: 1.3rem; font-weight: 600; margin-bottom: 1rem; color: var(--text-dark); }
        .feature p { color: var(--light-grey); }
        
        .specialty { background: rgba(255, 255, 255, 0.1); }
        .specialty-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 2rem; max-width: 1200px; margin: 0 auto; }
        .specialty-card { 
            padding: 2.5rem 2rem; 
            background: var(--secondary-bg-glass);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 8px; 
            text-align: center; 
            transition: all 0.3s; 
        }
        .specialty-card:hover { 
            border-color: var(--accent-primary); 
            transform: translateY(-5px); 
            background: rgba(255, 255, 255, 0.25); 
        }
        .specialty-emoji { font-size: 3rem; margin-bottom: 1rem; }
        .specialty-card h3 { font-size: 1.2rem; font-weight: 600; margin-bottom: 0.8rem; color: var(--text-dark); }
        .specialty-card p { color: var(--light-grey); font-size: 0.95rem; }
        
        .cta-section { background: transparent; text-align: center; }
        .cta-section h2 { font-size: clamp(2rem, 5vw, 3.5rem); font-weight: 700; margin-bottom: 1.5rem; color: var(--text-dark); }
        .cta-section p { font-size: 1.2rem; color: var(--light-grey); max-width: 600px; margin: 0 auto 3rem; }
        .cta-button { display: inline-block; background: var(--accent-primary); color: var(--white); padding: 1.1rem 3rem; border-radius: 6px; text-decoration: none; font-weight: 600; transition: all 0.3s; box-shadow: 0 4px 20px rgba(255, 94, 161, 0.4); }
        .cta-button:hover { transform: translateY(-2px); box-shadow: 0 10px 30px rgba(255, 94, 161, 0.6); }
        
        footer { 
            background: rgba(255, 255, 255, 0.1); 
            backdrop-filter: blur(10px);
            padding: 3rem 2rem; 
            border-top: 1px solid rgba(255, 255, 255, 0.2); 
            text-align: center; 
        }
        .footer-logo { display: inline-flex; align-items: center; gap: 1rem; margin-bottom: 2rem; text-decoration: none; }
        .footer-logo img { width: 40px; height: 40px; }
        .footer-logo span { font-size: 1.3rem; font-weight: 600; color: var(--text-dark); font-family: var(--font-display); }
        .footer-links { display: flex; justify-content: center; gap: 2rem; margin-bottom: 2rem; flex-wrap: wrap; }
        .footer-links a { color: var(--light-grey); text-decoration: none; transition: color 0.3s; }
        .footer-links a:hover { color: var(--text-dark); }
        .footer-copy { color: var(--light-grey); font-size: 0.85rem; }
        
        /* === START: New Chatbot Iframe Launcher CSS === */
        .chatbot-launcher-button {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            width: 70px;
            height: 70px;
            border-radius: 50%;
            background: var(--accent-primary);
            border: none;
            cursor: pointer;
            box-shadow: 0 8px 25px rgba(255, 94, 161, 0.4);
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            z-index: 1000;
        }
        .chatbot-launcher-button:hover {
            transform: scale(1.1);
            box-shadow: 0 12px 35px rgba(255, 94, 161, 0.6);
        }
        .chatbot-launcher-button i {
            font-size: 1.8rem;
            color: var(--white);
            position: absolute;
            transition: transform 0.3s, opacity 0.3s;
        }
        .chatbot-launcher-button .close-icon {
            display: block;
            opacity: 0;
            transform: rotate(-180deg);
        }
        .chatbot-launcher-button.active .chat-icon {
            transform: rotate(180deg);
            opacity: 0;
        }
        .chatbot-launcher-button.active .close-icon {
            opacity: 1;
            transform: rotate(0);
        }
        .chatbot-pulse {
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: var(--accent-primary);
            opacity: 0.5;
            animation: pulse 2s infinite;
            z-index: -1;
        }
        @keyframes pulse { 
            0%, 100% { transform: scale(1); opacity: 0.5; } 
            50% { transform: scale(1.3); opacity: 0; } 
        }

        .chatbot-iframe-container {
            position: fixed;
            bottom: 110px;
            right: 2rem;
            width: 400px;
            max-width: calc(100vw - 4rem);
            height: 600px;
            max-height: calc(100vh - 150px);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            border-radius: 20px;
            overflow: hidden;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            z-index: 999;
            transform: translateY(20px) scale(0.95);
            opacity: 0;
            pointer-events: none;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        .chatbot-iframe-container.active {
            opacity: 1;
            transform: translateY(0) scale(1);
            pointer-events: all;
        }
        .chatbot-iframe {
            width: 100%;
            height: 100%;
            border: none;
        }
        /* === END: New Chatbot Iframe Launcher CSS === */

        @media (max-width: 768px) {
            nav { padding: 1rem 1.5rem; }
            .nav-links { display: none; }
            section { padding: 4rem 1.5rem; }
            .hero h1 { font-size: 2.5rem; }
            .locations-grid, .features-grid, .specialty-grid { grid-template-columns: 1fr; }
            
            .chatbot-iframe-container {
                bottom: 90px;
                right: 1rem;
                width: calc(100vw - 2rem);
                height: calc(100vh - 120px);
            }
            .chatbot-launcher-button {
                width: 60px;
                height: 60px;
                bottom: 1rem;
                right: 1rem;
            }
        }
    </style>
</head>
<body>
    <header id="header">
        <nav>
            <a href="/" class="logo">
                <img src="https://github.com/kfischi/MULTIBRAWN-V.1/raw/main/Gallery/לוגו2.png" alt="MULTIBRAWN">
                <span>MULTIBRAWN</span>
            </a>
            <div class="nav-links">
                <a href="/">דף הבית</a>
                <a href="about.html">אודות</a>
                <a href="gallery.html">גלריה</a>
                <a href="contact.html" class="nav-cta">צור קשר</a>
            </div>
        </nav>
    </header>

    <section class="hero">
        <div class="hero-bg">
            <div class="hero-slide active">
                <img src="https://res.cloudinary.com/dptyfvwyo/image/upload/v1762004983/HERO_zb7pwc.jpg" alt="Hero">
            </div>
            <div class="hero-slide">
                <img src="https://raw.githubusercontent.com/kfischi/MULTIBRAWN-V.1/main/Gallery/Villa1.jpg" alt="Villa">
            </div>
            <div class="hero-slide">
                <img src="https://raw.githubusercontent.com/kfischi/MULTIBRAWN-V.1/main/Gallery/Villas/Villa_Wow.jpg" alt="Location">
            </div>
        </div>
        <div class="hero-content">
            <h1>לוקיישנים שווים במינימום מאמץ</h1>
            <h2>מולטיבראון - משנים את עולם החיפוש: יותר אישי, יותר ממוקד, פחות כאבי ראש.</h2>
            <a href="contact.html" class="hero-cta">בואו נתחיל</a>
        </div>
    </section>

    <section class="locations">
        <div class="section-header">
            <h2>סוגי לוקיישנים</h2>
            <p>כל מקום נבחר בקפידה ונבדק אישית על ידינו</p>
        </div>
        <div class="locations-grid">
            <a href="apartments.html" class="location-card-link">
                <div class="location-card">
                    <img src="https://res.cloudinary.com/dptyfvwyo/image/upload/v1760818934/20_t6yw8m.jpg" alt="פנטהאוז">
                    <div class="location-overlay">
                        <h3>דירות נופש ופנטהאוזים</h3>
                        <p>חיים עירוניים ברמה אחרת</p>
                    </div>
                </div>
            </a>
            <a href="villas.html" class="location-card-link">
                <div class="location-card">
                    <img src="https://res.cloudinary.com/dptyfvwyo/image/upload/v1760818934/22_tt9jvz.jpg" alt="וילה">
                    <div class="location-overlay">
                        <h3>וילות פרטיות</h3>
                        <p>מרחב, פרטיות ונוחות מלאה</p>
                    </div>
                </div>
            </a>
            <a href="events.html" class="location-card-link">
                <div class="location-card">
                    <img src="https://res.cloudinary.com/dptyfvwyo/image/upload/v1762003191/1_tsc6xx.jpg" alt="אירועים">
                    <div class="location-overlay">
                        <h3>מתחמי אירועים</h3>
                        <p>מקומות מושלמים לכל אירוע</p>
                    </div>
                </div>
            </a>
            <a href="zimmers.html" class="location-card-link">
                <div class="location-card">
                    <img src="https://res.cloudinary.com/dptyfvwyo/image/upload/v1760818996/Zimmer2_ge7g6h.jpg" alt="צימר">
                    <div class="location-overlay">
                        <h3>צימרים רומנטיים</h3>
                        <p>בריחה לטבע עם כל הפינוקים</p>
                    </div>
                </div>
            </a>
            <!-- Card for Hotels - assuming you have a page -->
            <a href="hotels.html" class="location-card-link">
                <div class="location-card">
                    <img src="https://res.cloudinary.com/dptyfvwyo/image/upload/v1760818995/Hotel1_ihkey7.jpg" alt="מלון">
                    <div class="location-overlay">
                        <h3>מלונות בוטיק</h3>
                        <p>מקומות עם אישיות ושירות אישי</p>
                    </div>
                </div>
            </a>
        </div>
    </section>

    <section class="features">
        <div class="section-header">
            <h2>השירות הייחודי שלנו: הסוכן האישי שלכם</h2>
            <p>חיפוש לוקיישן לא צריך להיות מסובך. תנו לנו לעשות את העבודה עבורכם.</p>
        </div>
        <div class="features-grid">
            <div class="feature">
                <div class="feature-icon"><i class="fas fa-clock"></i></div>
                <h3>חוסכים לכם זמן</h3>
                <p>תגידו לנו מה אתם מחפשים, ואנחנו נמצא את המקומות המתאימים. לא עוד שעות של גלילה באתרים.</p>
            </div>
            <div class="feature">
                <div class="feature-icon"><i class="fas fa-check-circle"></i></div>
                <h3>נבדקים אישית</h3>
                <p>כל מקום שאנחנו ממליצים עליו, אנחנו מכירים באופן אישי. אין הפתעות לא נעימות.</p>
            </div>
            <div class="feature">
                <div class="feature-icon"><i class="fas fa-key"></i></div>
                <h3>מאגר ייחודי</h3>
                <p>יש לנו גישה למקומות שלא תמצאו בחיפוש רגיל. לוקיישנים שזמינים רק דרכנו.</p>
            </div>
        </div>
    </section>

    <section class="specialty">
        <div class="section-header">
            <h2>מתמחים באירועים</h2>
            <p>לכל סוג אירוע יש את המקום המושלם</p>
        </div>
        <div class="specialty-grid">
            <div class="specialty-card">
                <div class="specialty-emoji">✡️</div>
                <h3>שבתות חתן ואירועים דתיים</h3>
                <p>מתחמים מותאמים למשפחות, קרבה לבתי כנסת ודרישות כשרות</p>
            </div>
            <div class="specialty-card">
                <div class="specialty-emoji">👥</div>
                <h3>כנסים וימי גיבוש</h3>
                <p>מקומות לקבוצות עם כל הציוד הנדרש לאירועים עסקיים</p>
            </div>
            <div class="specialty-card">
                <div class="specialty-emoji">💍</div>
                <h3>חתונות ואירועים פרטיים</h3>
                <p>לוקיישנים מושלמים לאירועי החיים החשובים</p>
            </div>
        </div>
    </section>

    <section class="cta-section">
        <h2>מוכנים למצוא את המקום המושלם?</h2>
        <p>ספרו לנו מה אתם מחפשים, ואנחנו נדאג לשאר</p>
        <a href="contact.html" class="cta-button">בואו נדבר</a>
    </section>

    <footer>
        <a href="/" class="footer-logo">
            <img src="https://github.com/kfischi/MULTIBRAWN-V.1/raw/main/Gallery/לוגו2.png" alt="MULTIBRAWN">
            <span>MULTIBRAWN</span>
        </a>
        <div class="footer-links">
            <a href="privacy.html">מדיניות פרטיות</a>
            <a href="accessibility.html">הצהרת נגישות</a>
            <a href="about.html">אודות</a>
            <a href="contact.html">צור קשר</a>
        </div>
        <p class="footer-copy">© 2025 MULTIBRAWN. כל הזכויות שמורות.</p>
    </footer>

    <div class="chatbot-iframe-container" id="chatbotWindow">
        <iframe src="chatbot.html" class="chatbot-iframe" title="מולטי AI"></iframe>
    </div>
    <button class="chatbot-launcher-button" id="chatbotButton">
        <div class="chatbot-pulse"></div>
        <i class="fas fa-comment-dots chat-icon"></i>
        <i class="fas fa-times close-icon"></i>
    </button>

    <script>
        // --- Header Scroll Effect ---
        const header = document.getElementById('header');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 100) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });

        // --- Hero Slider ---
        const slides = document.querySelectorAll('.hero-slide');
        let currentSlide = 0;
        
        if (slides.length > 0) {
            slides[0].classList.add('active');
            setInterval(() => {
                slides[currentSlide].classList.remove('active');
                currentSlide = (currentSlide + 1) % slides.length;
                slides[currentSlide].classList.add('active');
            }, 5000);
        }

        // --- Chatbot Iframe Launcher JS ---
        const chatbotButton = document.getElementById('chatbotButton');
        const chatbotWindow = document.getElementById('chatbotWindow');

        function toggleChatbot() {
            chatbotWindow.classList.toggle('active');
            chatbotButton.classList.toggle('active');
        }
        chatbotButton.addEventListener('click', toggleChatbot);
    </script>
</body>
</html>
